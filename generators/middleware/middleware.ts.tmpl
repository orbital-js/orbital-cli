/**
 * $CAMEL_CASE Middleware method generated by Boat CLI
 */
export function $CAMEL_CASE(req, res, next) {
    console.log("Request being handled through your custom middlewares.");
    next();
}

// Not passing through this middleware? Make sure to run `next` at the end of your implementation.